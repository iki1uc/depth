export const GPU = {

    rom(x) {
        return x + "_rom"
    },

    fusion(x) {
        return x + "_fusion"
    },

    render(x) {
        return x + "_render"
    },

    vitality(x) {
        return x + "_360v"
    }
}
